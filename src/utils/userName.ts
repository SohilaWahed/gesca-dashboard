export const getEmployeeFullName = (
  firstName: string,
  lastName: string
) => `${firstName} ${lastName}`;

export const avatarName = (
  firstName: string,
  lastName: string
) => `${firstName[0]}${lastName[0]}`;